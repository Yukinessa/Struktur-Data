/*
    NAMA : Kun Amrin Amanu
    NIM : A11.2017.10333
    PSDA 2 I
*/
#include "header.h"
#include "boolean.h"

int main()
{
    int i;

    CreateEmpty(&Q,5);
    Add(&Q,10);
    Add(&Q,11);
    Add(&Q,12);
    Add(&Q,13);
    Add(&Q,14);
    Add(&Q,15);

    Cetak(Q);
    Cetak(Q);
    return 0;
}
